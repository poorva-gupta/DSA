class Solution:
    def differenceOfSums(self, n: int, m: int) -> int:
        count=0
        total=0
        for i in range(1,n+1):
            if i%m!=0:
                count+=i
        for i in range(1,n+1):
            if i%m==0:
                total+=i
        return count-total
        
