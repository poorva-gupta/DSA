class Solution:
    def countTriples(self, n: int) -> int:
        squares=set(i*i for i in range(1,n+1))
        count=0
        for i in range(1,n):
            for j in range(1,n):
                if i*i +j*j in squares:
                    count+=1
        return count
        
