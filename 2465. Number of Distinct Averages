class Solution:
    def distinctAverages(self, nums: List[int]) -> int:
        nums.sort()
        a=set()
        start=0
        end=len(nums)-1
        while start<end:
            a.add((nums[start]+nums[end]))
            start+=1
            end-=1
        return len(a)
        
