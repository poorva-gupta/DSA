class Solution:
    def reorderedPowerOf2(self, n: int) -> bool:
        a=sorted(str(n))
        for i in range(31):
            if sorted(str(2**i))==a:
                return True
        return False
