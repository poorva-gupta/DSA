class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        curr=0
        maximum=0
        for i in gain:
            curr+=i
            maximum=max(curr,maximum)
        return maximum
        
